# Contributing

## Local setup

1. Use Node from `.nvmrc`.
2. Run `npm ci`.
3. Run `npm run dev`.

## Quality gates

- `npm run typecheck`
- `npm run lint`
- `npm run test`
- `npm run build`

Open pull requests against `main` with a clear summary and test notes.
